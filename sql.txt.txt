DROP DATABASE IF EXISTS 有效；
CREATE DATABASE Works DEFAULT CHARSET=GBK;

使用作品；
创建表用户信息
(
用户名 nvarchar(50) 主键，
密码 nvarchar(50) 不为空，
QQ nvarchar(12),
电子邮件 nvarchar(50),
输入 nchar(10) 不为空，
UserImg nvarchar(250)
);

创建表活动
(
ActivityID int(4) 主键 auto_increment，
ActivitName nvarchar(125) 不为空，
EndTime datetime(6) 不为空，
ActivityPicture nvarchar(250) 不为空，
活动介绍 nvarchar(8000),
摘要 nvarchar (250),
ActivityVerify nchar(10) not null DEFAULT '´ýÉóºË',
活动状态 nchar(10),
用户名 nvarchar(50) 不为空
);

创建表工作信息
(
WorkID int(4) 主键，
WorkName nvarchar(50) 不为空，
WorkPicture nvarchar(250) 不为空，
UploadTime datetime(6) 不为空，
工作介绍 nvarchar(8000),
WorkVerify nchar(10) not NULL DEFAULT '´ýÉóºË',
用户名 nvarchar(50) 不为空，
活动名称 nvarchar(125),
推荐 nchar(2) 不为空，
RecommendTime 日期时间(6)
);

CREATE TABLE COMmeut
(
CommentID int(4) PRIMARY KEY auto_increment,
WordID int(4) 不为空，
WordName nvarchar(50) 不为空，
用户名 nvarchar(50) 不为空，
CommentContent nvarchar(8000) 不为空，
CommentTime dateTime(6) 不为空
);

创建表图像
(
ImgID int(4) PRIMARY KEY auto_increment,
ImgUrl nvarchar(50) 不为空，
ImgText nvarchar(50) 不为空，
ImgLink nvarchar(100) 不为空，
ImgAlt nvarchar(50) 不为空
);
